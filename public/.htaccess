RewriteEngine On
# On définit la base à partir de laquelle on réécrit
RewriteBase /test/public/

# Si le fichier ou le dossier n'existe pas, on redirige vers index.php
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]